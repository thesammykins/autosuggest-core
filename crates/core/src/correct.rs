//! Failed-command correction rule engine + native predicates.
//!
//! Evaluates JSON correction rules and native Rust predicates against a failed
//! command's context to propose ranked rewrites. Implemented in M3
//! (see `TECH.md §3.5`).

// M3+: correction rule engine + native predicates.
