//! Filtering and scoring of completion candidates.
//!
//! Prefix/fuzzy filtering plus scoring by `priority` and recency
//! (see `TECH.md §3.3`). Implemented in M1.

// M1+: prefix/fuzzy filtering + priority/recency scoring.
