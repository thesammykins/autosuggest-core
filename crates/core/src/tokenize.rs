//! Command-line tokenizer.
//!
//! Splits a raw input line plus a cursor byte-offset into tokens and identifies
//! the token under the cursor. Implemented in M1 (see `ROADMAP.md`).

// M1+: tokenizer + cursor-token classification.
